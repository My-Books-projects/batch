<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mybooks.batch.usercoupon.mapper.UserCouponMapper">
    <insert id="createUserBirthDayCoupon">
        INSERT INTO user_coupon(user_id, coupon_id, user_coupon_created_date, user_coupon_date, is_used)
        VALUES (#{userId}, #{couponId}, #{userCouponCreatedDate}, #{userCouponDate}, #{isUsed})
    </insert>
</mapper>
